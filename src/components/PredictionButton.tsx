import { StyleSheet, Text, View } from "react-native";
import React from "react";

const PredictionButton: React.FC = () => {
	return (
		<View>
			<Text>PredictionButton</Text>
		</View>
	);
};

export default PredictionButton;

const styles = StyleSheet.create({});
